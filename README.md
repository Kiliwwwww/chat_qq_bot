# QQ Bot

基于 NoneBot2 的 QQ 机器人

## 快速开始

1. 激活虚拟环境:
```bash
source .venv/bin/activate
```

2. 运行机器人:
```bash
python bot.py
```

3. 访问文档:
http://localhost:8080/website/

## 插件

- `echo`: 复读机插件，使用 `/echo <消息>` 或 `/复读 <消息>`

## 配置

编辑 `.env` 文件配置机器人参数。